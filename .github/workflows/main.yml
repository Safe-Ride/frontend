name: Send Email on Commit

on:
  push:

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Send email notification via SendGrid
        uses: benc-uk/sendgrid-action@v2
        with:
          sendgrid_api_key: ${{ secrets.SENDGRID_API_KEY }}
          from: 'safe20.ride24@gmail.com'
          to: ${{ github.event.head_commit.author.email }}
          subject: '⚠️ Lembrete de Relatório de produtividade ⚠️'
          content: |
            Olá, ${{ github.event.head_commit.author.name }}.

            Você acabou de fazer um commit no repositório.
            Não se esqueça de fazer o relatório de produtividade para não ter problemas com apontamentos e acumular faltas em PI.

            🚨 Atenção 🚨

