name: Send Email on Commit

on:
  push:

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Atualize para v4 para suportar Node 20

      - name: Send email notification
        uses: dawidd6/action-send-mail@v4  # Verifique se a v4 existe, caso contrário, mantenha v3
        with:
          server_address: in-v3.mailjet.com
          server_port: 465
          username: ${{ secrets.MAILJET_API_KEY }}
          password: ${{ secrets.MAILJET_SECRET_KEY }}
          subject: '⚠️ Lembrete de Relatório de produtividade ⚠️'
          body: |
            Olá, ${{ github.event.head_commit.author.name }}.

            Você acabou de fazer um commit no repositório.
            Não se esqueça de fazer o relatório de produtividade para não ter problemas com apontamentos
            e acumular faltas em PI.

            🚨 Atenção 🚨
          to: ${{ github.event.head_commit.author.email }}
          from: ${{ secrets.EMAIL_USERNAME }}

