name: Send Email on Commit

on:
  push:

jobs:
  send-email:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: in-v3.mailjet.com  # Servidor SMTP do Mailjet
          server_port: 587  
          username: ${{ secrets.MAILJET_API_KEY }}  # API Key do Mailjet
          password: ${{ secrets.MAILJET_SECRET_KEY }}  # Secret Key do Mailjet
          subject: '⚠️ Lembrete de Relatório de produtividade ⚠️'
          body: |
            Olá, ${{ github.event.pusher.email }}.

            Você acabou de fazer um commit no repositório.
            Não se esqueça de fazer o relatório de produtividade para não ter problemas com apontamentos
            e acumular faltas em PI.

            🚨 Atenção 🚨
          to: ${{ github.event.pusher.email }}  # Enviar e-mail para o autor do commit
          from: ${{ secrets.EMAIL_USERNAME }}  # Defina o remetente como um e-mail válido
